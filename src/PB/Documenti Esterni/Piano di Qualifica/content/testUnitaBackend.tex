\subsubsection{Test di Unità Backend}

\renewcommand{\arraystretch}{1.2}
\begin{longtable}{|
		>{\raggedright\arraybackslash}p{0.15\columnwidth} |
		>{\raggedright\arraybackslash}p{0.325\columnwidth} |
		>{\raggedright\arraybackslash}p{0.325\columnwidth} |
		>{\centering\arraybackslash}p{0.10\columnwidth} |
	}
	\hline
	\rowcolor[gray]{0.9}
	\textbf{Codice Test$^G$} & \textbf{Descrizione} & \textbf{Valore atteso} & \textbf{Stato} \\
	\hline
	\endhead
	TU-B\_01 & Implementato da \textit{\seqsplit{test\_registration\_success}}; verifica che l'endpoint di registrazione dell'api possa essere chiamato e ritorni il valore una risposta positiva & \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Risposta HTTP con stato 201
		\item Il campo "ok", della risposta in formato Json, è impostato a "True"
	\end{itemize} & S \\
	\hline
	TU-B\_02 & Implementato da \texttt{\seqsplit{test\_register\_user\_twice}}; verifica che l'endpoint di registrazione dell'api possa essere chiamato e che non ci sia una registrazione duplicata & \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Risposta HTTP con stato 400
		\item Il campo "ok" dentro l'oggetto "detail", della risposta in formato Json, è impostato a "False"
	\end{itemize} & S \\
	\hline
	TU-B\_03 & Implementato da \texttt{\seqsplit{test\_registration\_missing\_fields}}; verifica che l'endpoint di registrazione dell'api possa essere chiamato e che in caso di campi mancanti restiTU-Bisce una risposta negativa & Risposta HTTP con stato 422 & S \\
	\hline
	TU-B\_04 & Implementato da \texttt{\seqsplit{test\_register\_invalid\_email\_domain}}; verifica che l'endpoint di registrazione dell'api possa essere chiamato e verifica il caso in cui l'utente inserisce una main in un formato non corretto & \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Risposta HTTP con stato 400
		\item "email" come prima parola della stringa contenuta dal campo "errors" nell'oggetto "detail"
	\end{itemize} & S \\
	\hline
	TU-B\_05 & Implementato da \texttt{\seqsplit{test\_register\_email\_already\_exists}}; verifica che l'endpoint di registrazione dell'api possa essere chiamato e verifica il caso in cui l'utente inserisce una mail che è già presente nel sistema & \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Risposta HTTP con stato 400
		\item "Email" come prima parola della stringa contenuta dal campo "errors" nell'oggetto "detail"
	\end{itemize} & S \\
	\hline
	TU-B\_07 & Implementato da \texttt{\seqsplit{test\_login\_success}}; verifica che l'endpoint di autenticazione dell'api possa essere chiamato e verifica la presenza di un utente nel sistema &  \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Risposta HTTP con stato 200
		\item Il campo "ok", della risposta in formato Json, è impostato a "True"
	\end{itemize} & S \\
	\hline
	TU-B\_08 & Implementato da \texttt{\seqsplit{test\_login\_failed}}; verifica che l'endpoint di autenticazione dell'api possa essere chiamato e verifica che il sistemi restiTU-Bisca una risposta negativa all'invio delle credenziali di un utente non registrato & Risposta HTTP con stato 400 & S \\
	\hline
	TU-B\_09 & Implementato da \texttt{\seqsplit{test\_login\_missing\_fields}}; verifica che l'endpoint di autenticazione dell'api possa essere chiamato e in caso di campi mancanti restiTU-Bisce una risposta negativa & Risposta HTTP con stato 422 & S \\
	\hline
	TU-B\_10 & Implementato da \texttt{\seqsplit{test\_reTU-Brns\_users\_service\_instance}}; verifica che la funzione \texttt{\seqsplit{get\_user\_service}} restiTU-Bisca un'istanza della classe UserService & Istanza di \texttt{UserService} restiTU-Bita correttamente & S \\
	\hline
	TU-B\_11 & Implementato da \texttt{\seqsplit{test\_user\_service\_wraps\_user\_repo\_adapter}}; verifica che l'attributo \texttt{repo} dell'istanza della classe \texttt{UserService} sia correttamente configurato & Attributo \texttt{repo} correttamente configurato come \texttt{UserRepoAdapter} & S \\
	\hline
	TU-B\_12 & Implementato da \texttt{\seqsplit{test\_adapter\_receives\_db\_session}}; verifica che il db iniettato nello \texttt{UserService} sia configurato correttamente & Db iniettato e configurato correttamente & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestGetCurrenTU-Bser} \\
	\hline
	TU-B\_13 & Implementato da \texttt{\seqsplit{test\_valid\_token\_reTU-Brns\_username}}; verifica che il recupero dell'identità utente, funzione \texttt{get\_current\_user} tramite cookie di sessione & Username corretto dopo la decodifica del token & S \\
	\hline
	TU-B\_14 & Implementato da \texttt{\seqsplit{test\_invalid\_token\_raises\_401}}; verifica che l'accesso venga negato, con errore HTTP, se il token nel cookie è invalido o nullo & Risposta HTTP con stato 401 & S \\
	\hline
	TU-B\_15 & Implementato da \texttt{\seqsplit{test\_invalid\_token\_detail\_message}}; verifica che l'accesso venga negato, verificando che il messaggio di errore sia corretto, in caso di token invalido o nullo & Il campo \texttt{detail} assume valore \textit{Token non valido} & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestDeleteAccount} \\
	\hline
	TU-B\_16 & Implementato da \texttt{\seqsplit{test\_delete\_account\_success}}; verifica che l'endpoint di eliminazione del cliente possa essere chiamato e verifica che l'eliminazione vada a buon fine & \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Risposta HTTP con stato 200
		\item Il campo "ok", della risposta in formato Json, è impostato a "True"
		\item Il campo "errors", della risposta in formato Json, è vuoto
	\end{itemize} & S \\
	\hline 
	TU-B\_17 & Implementato da \texttt{\seqsplit{test\_delete\_account\_user\_not\_found}}; verifica che l'endpoint di eliminazione del cliente possa essere chiamato e verifica che, fornito un utente non valido, il test fallisca & \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Risposta HTTP con stato 404
		\item Il campo "ok", della risposta in formato Json, è impostato a "False"
		\item Il campo "errors", della risposta in formato Json, contiene "Utente non trovato"
	\end{itemize} & S \\
	\hline
	TU-B\_18 & Implementato da \texttt{\seqsplit{test\_delete\_account\_deletion\_error}}; verifica che l'endpoint di eliminazione del cliente possa essere chiamato e verifica che l'endpoint sollevi un'eccezione qualora l'eliminazione non vada a buon fine & \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Risposta HTTP con stato 500
		\item Il campo "ok", della risposta in formato Json, è impostato a "False"
		\item Il campo "errors", della risposta in formato Json, contiene "Errore durante la cancellazione"
	\end{itemize} & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestEmailDomainExists} \\
	\hline
	TU-B\_19 & Implementato da \texttt{\seqsplit{test\_reTU-Brns\_true\_when\_mx\_record\_found}}; verifica, tramite una simulazione di una risoluzione DNS positiva, che la mail interessata esista nel sistema & Risposta positiva da parte del sistema nella query di esistenza della mail nel dominio & S \\
	\hline
	TU-B\_20 & Implementato da \texttt{\seqsplit{test\_reTU-Brns\_false\_when\_mx\_record\_not\_found}}; verifica, tramite una simulazione di una risoluzione DNS positiva, che la main interessata non sia esistente nel sistema & \begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
		\item Eccezione sollevata
		\item Risposta negativa da parte del sistema nella query di esistenza della mail nel dominio
	\end{itemize} & S \\
	\hline
	TU-B\_21 & Implementato da \texttt{\seqsplit{test\_extracts\_domain\_from\_email}}; verifica che il dominio venga correttamente estratto dall'email e passato al resolver DNS con i parametri corretti & La chiamata a \texttt{resolve} avviene una sola volta con il dominio corretto & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestUsernameAlreadyExistsError} \\
	\hline
	TU-B\_22 & Implementato da \texttt{{\seqsplit{test\_is\_value\_error}}}; verifica che la classe \texttt{\seqsplit{UsernameAlreadyExistsError}} istanzi un oggetto di tipo \texttt{ValueError} & Istanza di \texttt{ValueError} & S \\
	\hline
	TU-B\_23 & Implementato da \texttt{\seqsplit{test\_message}}; verifica che il messaggio di errore dell'oggetto di tipo \texttt{\seqsplit{UsernameAlreadyExistsError}} sia correttamente configurato & Una stringa dal valore "Username già esistente" & S \\
	\hline
	TU-B\_24 & Implementato da \texttt{\seqsplit{test\_can\_be\_raised\_and\_caught}}; verifica che la classe \texttt{\seqsplit{UsernameAlreadyExistsError}} possa essere lanciata come eccezione & Eccezione di tipo \texttt{\seqsplit{UsernameAlreadyExistsError}} lanciata & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestInvalidEmailFormatError} \\
	\hline
	TU-B\_25 & Implementato da \texttt{{\seqsplit{test\_is\_value\_error}}}; verifica che la classe \texttt{\seqsplit{InvalidEmailFormatError}} istanzi un oggetto di tipo \texttt{ValueError} & Istanza di \texttt{ValueError} & S \\
	\hline
	TU-B\_26 & Implementato da \texttt{\seqsplit{test\_message}}; verifica che il messaggio di errore dell'oggetto di tipo \texttt{\seqsplit{InvalidEmailFormatError}} sia correttamente configurato & Una stringa dal valore "L'email non è nel formato corretto" & S \\
	\hline
	TU-B\_27 & Implementato da \texttt{\seqsplit{test\_can\_be\_raised\_and\_caught}}; verifica che la classe \texttt{\seqsplit{InvalidEmailFormatError}} possa essere lanciata come eccezione & Eccezione di tipo \texttt{\seqsplit{InvalidEmailFormatError}} lanciata & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestEmailAlreadyExistsError} \\
	\hline
	TU-B\_28 & Implementato da \texttt{{\seqsplit{test\_is\_value\_error}}}; verifica che la classe \texttt{\seqsplit{EmailAlreadyExistsError}} istanzi un oggetto di tipo \texttt{ValueError} & Istanza di \texttt{ValueError} & S \\
	\hline
	TU-B\_29 & Implementato da \texttt{\seqsplit{test\_message}}; verifica che il messaggio di errore dell'oggetto di tipo \texttt{\seqsplit{EmailAlreadyExistsError}} sia correttamente configurato & Una stringa dal valore "Email già esistente" & S \\
	\hline
	TU-B\_30 & Implementato da \texttt{\seqsplit{test\_can\_be\_raised\_and\_caught}}; verifica che la classe \texttt{\seqsplit{EmailAlreadyExistsError}} possa essere lanciata come eccezione & Eccezione di tipo \texttt{\seqsplit{EmailAlreadyExistsError}} lanciata & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestInvalidCredentialsError} \\
	\hline
	TU-B\_31 & Implementato da \texttt{{\seqsplit{test\_is\_value\_error}}}; verifica che la classe \texttt{\seqsplit{InvalidCredentialsError}} istanzi un oggetto di tipo \texttt{ValueError} & Istanza di \texttt{ValueError} & S \\
	\hline
	TU-B\_32 & Implementato da \texttt{\seqsplit{test\_message}}; verifica che il messaggio di errore dell'oggetto di tipo \texttt{\seqsplit{InvalidCredentialsError}} sia correttamente configurato & Una stringa dal valore "Username o password errati" & S \\
	\hline
	TU-B\_33 & Implementato da \texttt{\seqsplit{test\_can\_be\_raised\_and\_caught}}; verifica che la classe \texttt{\seqsplit{InvalidCredentialsError}} possa essere lanciata come eccezione & Eccezione di tipo \texttt{\seqsplit{InvalidCredentialsError}} lanciata & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestUserCreationError} \\
	\hline
	TU-B\_34 & Implementato da \texttt{\seqsplit{test\_is\_exception}}; verifica che la classe \texttt{\seqsplit{InvalidCredentialsError}} istanzi un oggetto di tipo \texttt{Exception} & Istanza di \texttt{Exception} & S \\
	\hline
	TU-B\_35 & Implementato da \texttt{{\seqsplit{test\_is\_not\_value\_error}}}; verifica che la classe \texttt{\seqsplit{UserCreationError}} non istanzi un oggetto di tipo \texttt{ValueError} & Istanza di un oggetto diverso da \texttt{ValueError} & S \\
	\hline
	TU-B\_36 & Implementato da \texttt{\seqsplit{test\_message}}; verifica che il messaggio di errore dell'oggetto di tipo \texttt{\seqsplit{UserCreationError}} sia correttamente configurato & Una stringa dal valore "Errore durante la registrazione" & S \\
	\hline
	TU-B\_37 & Implementato da \texttt{\seqsplit{test\_can\_be\_raised\_and\_caught}}; verifica che la classe \texttt{\seqsplit{UserCreationError}} possa essere lanciata come eccezione & Eccezione di tipo \texttt{\seqsplit{UserCreationError}} lanciata & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestHashPassword} \\
	\hline
	TU-B\_38 & Implementato da \texttt{\seqsplit{test\_returns\_string}}; verifica che la funzione di hashing ritorni un valore di tipo stringa & Valore di tipo stringa & S \\
	\hline
	TU-B\_39 & Implementato da \texttt{\seqsplit{test\_hash\_is\_different\_from\_plain}}; verifica che la funzione di hashing ritori un valore diverso dalla password in chiaro & Valore diverso da password in chiaro & S \\
	\hline
	TU\_40 & Implementato da \texttt{\seqsplit{test\_delegates\_to\_bcrypt}}; verifica che la funzione deleghi correttamente l'operazione di hashing alla libreria \texttt{bcrypt} & Chiamata a \texttt{bcrypt.hashpw} effettuata esattamente una volta con i parametri previsti & S \\
	\hline
	\multicolumn{4}{|c|}{\rule{0pt}{15pt}\large TestVerifyPassword} \\
	\hline
	TU\_41 & Implementato da \texttt{\seqsplit{test\_correct\_password\_returns\_true}}; verifica che la funzione di validazione password riconosca una password confrontandola con il relativo hash & Valore booleano \texttt{True} & S \\
	\hline
	TU\_42 & Implementato da \texttt{\seqsplit{test\_wrong\_password\_returns\_false}}; verifica che la funzione di validazione non riconosca una password confrontandola con l'hash che dovrebbe assumere & Valore booleano \texttt{False} & S \\
	\hline
	TU\_43 & Implementato da \texttt{\seqsplit{test\_none\_hash\_returns\_false\_without\_calling\_bcrypt}}; verifica che in caso di hash vuoto, la funzione di verifica termina e non viene chiamata la libreria \texttt{bcrypt} & Valore booleano \texttt{False} & S \\
	\hline
	TU\_44 & Implementato da \texttt{\seqsplit{test\_empty\_password\_delegates\_to\_bcrypt}}; verifica che, anche in caso di password vuota, la libreria \texttt{bcrypt} viene comunque chiamata per il confronto booleano & Valore booleano \texttt{False} & S \\
	\hline
	TU\_45 & Implementato da \texttt{\seqsplit{test\_returns\_all\_results}}; verifica che la query eseguita dal database ritorni tutti i risultati. & Lista di valori \texttt{str} & S \\
	\hline
	TU\_46 & Implementato da \texttt{\seqsplit{test\_calls\_exec\_with\_stmt}}; verifica che il database esegua esattamente la query che gli viene passata nel parametro. \textit{stmt} & Valore booleano & S \\
	\hline
	TU\_47 & Implementato da \texttt{\seqsplit{test\_returns\_empty\_sequence}}; verifica che il database, in caso di risultato privo di tuple, ritorni correttamente una lista vuota.  & Lista priva di valori & S \\
	\hline
	TU\_48 & Implementato da \texttt{\seqsplit{test\_returns\_single\_result}}; verifica che il database, quando viene chiesto di restituire solo un risultato dalla query, restituisca correttamente il primo risultato. & Valore di tipo \texttt{str} & S \\
	\hline
	TU\_49 & Implementato da \texttt{\seqsplit{test\_returns\_none\_when\_not\_found}}; verifica che il database, in caso di risultato privo di tuple, quando viene chiesto di restituire un risultato restituisca correttamente \textit{None}. & Valore di tipo \texttt{None}& S \\
	\hline
	TU\_50 & Implementato da \texttt{\seqsplit{test\_calls\_exec\_with\_stmt}}; verifica che il database, quando viene chiesto di restituire un risultato esegua esattamente la query che gli viene passata nel parametro \textit{stmt}. & Valore booleano& S \\
	\hline
	TU\_51 & Implementato da \texttt{\seqsplit{test\_returns\_true\_on\_success}}; verifica che il database, quando viene chiesto di eseguire una query che coinvolga una mutazione, restituisca \text{True} in caso di esito positivo. & Valore booleano \texttt{True} & S \\
	\hline
	TU\_52 & Implementato da \texttt{\seqsplit{test\_commits\_on\_success}}; verifica che il database, quando viene chiesto di eseguire una query che coinvolga una mutazione, in caso di esito positivo chiami il metodo \texttt{commit}.& Valore booleano & S \\
	\hline
	TU\_53 & Implementato da \texttt{\seqsplit{test\_returns\_false\_on\_integrity\_error}}; verifica che il database, quando viene chiesto di eseguire una query che coinvolga una mutazione, in caso di esito negativo restituisca \textit{False}.& Valore booleano \texttt{False} & S \\
	\hline
	TU\_54 & Implementato da \texttt{\seqsplit{test\_rollback\_on\_integrity\_error}}; verifica che il database, quando viene chiesto di eseguire una query che coinvolga una mutazione, in caso di esito negativo chiami il metodo \texttt{rollback}.& Valore booleano & S \\
	\hline
	TU\_55 & Implementato da \texttt{\seqsplit{test\_returns\_false\_on\_generic\_exception}}; verifica che il database, quando viene chiesto di eseguire una query che coinvolga una mutazione, in caso di una eccezione generica restituisca \textit{False}.& Valore booleano \texttt{False} & S \\
	\hline
	TU\_56 & Implementato da \texttt{\seqsplit{test\_rollback\_on\_generic\_exception}}; verifica che il database, quando viene chiesto di eseguire una query che coinvolga una mutazione, in caso di una eccezione generica chiami il metodo \texttt{rollback}.& Valore booleano & S \\
	\hline
	TU\_57 & Implementato da \texttt{\seqsplit{test\_no\_commit\_on\_failure}}; verifica che il database, quando viene chiesto di eseguire una query che coinvolga una mutazione, in caso di esito negativo non chiami il metodo \texttt{commit}.& Valore booleano & S \\
	\hline
	TU\_58 & Implementato da \texttt{\seqsplit{test\_returns\_false\_on\_integrity\_error\_raw}}; verifica che il database, quando viene chiesto di eseguire una mutazione diretta, in caso di esito negativo restituisca \textit{False}. & Valore booleano \texttt{False} & S \\
	\hline
	TU\_59 & Implementato da \texttt{\seqsplit{test\_rollback\_on\_integrity\_error}}; verifica che il database, quando viene chiesto di eseguire una mutazione diretta, in caso di esito negativo chiami il metodo \texttt{rollback}. & Valore booleano & S \\
	\hline
	TU\_60 & Implementato da \texttt{\seqsplit{test\_execute\_raises\_not\_implemented}}; verifica la classe \texttt{Query}, quando viene chiamato il suo metodo \texttt{execute} lanci un'eccezione \texttt{NotImplementedException}. & Eccezione di tipo \texttt{NotImplementedError} lanciata & S \\
	\hline
	TU\_61 & Implementato da \texttt{\seqsplit{test\_execute\_raises\_not\_implemented}}; verifica la classe \texttt{Mutation}, quando viene chiamato il suo metodo \texttt{execute} lanci un'eccezione \texttt{NotImplementedException}. & Eccezione di tipo \texttt{NotImplementedError} lanciata & S \\
	\hline
	TU\_62 & Implementato da \texttt{\seqsplit{test\_valid}}; verifica che lo schema \texttt{UserSchema} sia valido. & Valore booleano & S \\
	\hline
	TU\_63 & Implementato da \texttt{\seqsplit{test\_missing\_username}}; verifica che, se lo schema \texttt{UserSchema} manca del campo \texttt{Username}, venga lanciata una eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} lanciata & S \\
	\hline
	TU\_64 & Implementato da \texttt{\seqsplit{test\_missing\_password}}; verifica che, se lo schema \texttt{UserSchema} manca del campo \texttt{Password}, venga lanciata una eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} lanciata & S \\
	\hline
	TU\_65 & Implementato da \texttt{\seqsplit{test\_ok\_no\_token}}; verifica che una \texttt{AuthResponse} possa essere creata anche senza campo \texttt{Token}. & Valore di tipo \texttt{None} & S \\
	\hline
	TU\_66 & Implementato da \texttt{\seqsplit{test\_ok\_with\_token}}; verifica che una \texttt{AuthResponse} possa essere creata con campo \texttt{Token}, di tipo \texttt{str}. & Valore di tipo \texttt{str} & S \\
	\hline
	TU\_67 & Implementato da \texttt{\seqsplit{test\_ok\_no\_token}}; verifica che una \texttt{AuthResponse} possa essere creata con campo \texttt{Errors}, di tipo \texttt{str}. In questo caso il campo \texttt{Ok} della response deve essere \texttt{None} e \texttt{r.errors} deve restituire l'errore & Valore di tipo \texttt{None}, valore di tipo \texttt{str}. & S \\
	\hline
	TU\_68 & Implementato da \texttt{\seqsplit{test\_not\_ok\_with\_errors}}; verifica che una \texttt{AuthResponse} possa essere creata con campo \texttt{Errors}, di tipo \texttt{str}. In questo caso il campo \texttt{Ok} della response deve essere \texttt{None} e \texttt{r.errors} deve restituire l'errore & Valore di tipo \texttt{None}, valore di tipo \texttt{str}. & S \\
	\hline
	TU\_69 & Implementato da \texttt{\seqsplit{test\_valid}}; verifica che lo schema \texttt{UserRegistrationSchema}  sia valido & Valore booleano. & S \\
	\hline
	TU\_70 & Implementato da \texttt{\seqsplit{test\_too\_short}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Username} troppo corto (meno di 4 caratteri), lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Username} & S \\
	\hline
	TU\_71 & Implementato da \texttt{\seqsplit{test\_too\_long}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Username} troppo lungo (più di 24 caratteri), lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Username} & S \\
	\hline
	TU\_72 & Implementato da \texttt{\seqsplit{test\_special\_chars\_not\_allowed}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Username} contenente caratteri speciali, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Username} & S \\
	\hline
	TU\_73 & Implementato da \texttt{\seqsplit{test\_exactly\_4\_chars}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Username} lungo 4 caratteri, contenga un campo \texttt{Username} uguale al valore con la quale è stata creata. & Valore di tipo \texttt{str} & S \\
	\hline
	TU\_74 & Implementato da \texttt{\seqsplit{test\_valid}}; verifica che lo schema \texttt{UserRegistrationSchema} sia valido e il campo \texttt{Password} corrisponda a quello inserito. & Valore booleano & S \\
	\hline
	TU\_75 & Implementato da \texttt{\seqsplit{test\_too\_short}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Password} troppo corto, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Password} & S \\
	\hline
	TU\_76 & Implementato da \texttt{\seqsplit{test\_too\_long}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Password} troppo lungo, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Password} & S \\
	\hline
	TU\_77 & Implementato da \texttt{\seqsplit{test\_missing\_uppercase}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Password} senza almeno una lettera maiuscola, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Password} & S \\
	\hline
	TU\_78 & Implementato da \texttt{\seqsplit{test\_missing\_lowercase}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Password} senza almeno una lettera minuscola, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Password} & S \\
	\hline
	TU\_79 & Implementato da \texttt{\seqsplit{test\_missing\_digit}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Password} senza almeno una cifra, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Password} & S \\
	\hline
	TU\_80 & Implementato da \texttt{\seqsplit{test\_missing\_special\_char}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Password} senza almeno un carattere speciale, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Password} & S \\
	\hline
	TU\_81 & Implementato da \texttt{\seqsplit{test\_valid}}; verifica che lo schema \texttt{UserRegistrationSchema} sia valido e il campo \texttt{Email} corrisponda a quello inserito. & Valore booleano & S \\
	\hline
	TU\_82 & Implementato da \texttt{\seqsplit{test\_missing\_at}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Email} senza un simbolo "chiocciola", lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Email} & S \\
	\hline
	TU\_83 & Implementato da \texttt{\seqsplit{test\_missing\_dot\_in\_domain}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Email} senza un simbolo "punto" dopo la sezione di dominio, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Email} & S \\
	\hline
	TU\_84 & Implementato da \texttt{\seqsplit{test\_spaces\_not\_allowed}}; verifica che lo schema \texttt{UserRegistrationSchema}, quando creato con un campo \texttt{Email} contenente spazi, lanci un'eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError} con parametro \texttt{Email} & S \\
	\hline
	TU\_85 & Implementato da \texttt{\seqsplit{test\_password\_match}}; verifica che lo schema \texttt{UserRegistrationSchema} contenga un campo \texttt{confirmPwd} uguale al campo \texttt{Password} inserito. & Valore booleano & S \\
	\hline
	TU\_86 & Implementato da \texttt{\seqsplit{test\_password\_mismatch}}; verifica che lo schema \texttt{UserRegistrationSchema} quando presenta un campo \texttt{confirmPwd} non uguale al campo \texttt{Password} inserito, lanci una eccezione di tipo \texttt{ValidationError}. & Eccezione di tipo \texttt{ValidationError}& S \\
	\hline
	TU\_87 & Implementato da \texttt{\seqsplit{test\_returns\_string}}; verifica che la classe \texttt{TokenUtility}, quando viene chiamato il suo metodo \texttt{create_token}, restituisca un valore di tipo \texttt{str} & Valore booleano & S \\
	\hline
	TU\_88 & Implementato da \texttt{\seqsplit{test\_token\_contains\_username}}; verifica che, quando si decodifica un token attraverso il metodo \texttt{decode}, venga resituito un dizionario contenente anche lo username con il quale il token è stato generato. & Valore booleano & S \\
	\hline
	TU\_89 & Implementato da \texttt{\seqsplit{test\_token\_has\_expiry}}; verifica che, quando si decodifica un token attraverso il metodo \texttt{decode}, venga resituito un dizionario contenente anche il valore indicizzato dalla chiave \texttt{exp}. & Valore booleano & S \\
	\hline
	TU\_90 & Implementato da \texttt{\seqsplit{test\_different\_users\_produce\_different\_tokens}}; verifica che la classe \texttt{TokenUtility}, quando viene chiamato il suo metodo \texttt{create_token}, restituisca valori diversi per username diversi. & Valore booleano & S \\
	\hline
	TU\_91 & Implementato da \texttt{\seqsplit{test\_valid\_token\_returns\_username}}; verifica che la classe \texttt{TokenUtility}, quando viene chiamato il suo metodo \texttt{decode_token}, restituisca lo username che è stato usato per creare il token. & Valore booleano & S \\
	\hline
	TU\_92 & Implementato da \texttt{\seqsplit{test\_expired\_token\_returns\_none}}; verifica che un token scaduto restituisca \texttt{None} quando viene decodificato. & Valore di tipo \texttt{None}& S \\
	\hline
	TU\_93 & Implementato da \texttt{\seqsplit{test\_invalid\_token\_returns\_none}}; verifica che un token non valido restituisca \texttt{None} quando viene decodificato. & Valore di tipo \texttt{None}& S \\
	\hline
	TU\_94 & Implementato da \texttt{\seqsplit{test\_empty\_string\_returns\_none}}; verifica che un token vuoto restituisca \texttt{None} quando viene decodificato. & Valore di tipo \texttt{None}& S \\
	\hline
	TU\_95 & Implementato da \texttt{\seqsplit{test\_token\_signed\_with\_wrong\_key\_retunrs\_none}}; verifica che un token firmato con una chiave sbagliata restituisca \texttt{None} quando viene decodificato. & Valore di tipo \texttt{None}& S \\
	\hline
	TU\_96 & Implementato da \texttt{\seqsplit{test\_roundtrip\_create\_and\_decode}}; verifica che un token valido che viene successivamente decodificato restituisca il valore usato per creare il token. & Valore di tipo \texttt{str} & S \\
	\hline
	TU\_97 & Implementato da \texttt{\seqsplit{test\_returns\_utente\_when\_found}}; verifica che l'adapter, quando presente un \texttt{Utente} con il valore \texttt{username} richiesto nel database, restituisca un oggetto \texttt{User} relativo a quell'utente. & Valore di tipo \texttt{User} & S \\
	\hline
	TU\_98 & Implementato da \texttt{\seqsplit{test\_returns\_none\_when\_not\_found}}; verifica che l'adapter, quando non presente un \texttt{Utente} con il valore \texttt{username} richiesto nel database, restituisca \texttt{None}. & Valore di tipo \texttt{None} & S \\
	\hline
	TU\_99 & Implementato da \texttt{\seqsplit{test\_returns\_true\_when\_found}}; verifica che il metodo \texttt{username_exists} dell'adapter, quando presente un \texttt{Utente} con il valore \texttt{username} richiesto nel database, restituisca \texttt{True}. & Valore booleano & S \\
	\hline
	TU\_100 & Implementato da \texttt{\seqsplit{test\_returns\_false\_when\_not\_found}}; verifica che il metodo \texttt{username_exists} dell'adapter, quando non presente un \texttt{Utente} con il valore \texttt{username} richiesto nel database, restituisca \texttt{False}. & Valore booleano & S \\
	\hline
	TU\_101 & Implementato da \texttt{\seqsplit{test\_returns\_true\_when\_found}}; verifica che il metodo \texttt{email_exists} dell'adapter, quando presente un \texttt{Utente} con il valore \texttt{email} richiesto nel database, restituisca \texttt{True}. & Valore booleano & S \\
	\hline
	TU\_102 & Implementato da \texttt{\seqsplit{test\_returns\_false\_when\_not\_found}}; verifica che il metodo \texttt{email_exists} dell'adapter, quando non presente un \texttt{Utente} con il valore \texttt{email} richiesto nel database, restituisca \texttt{False}. & Valore booleano & S \\
	\hline
	TU\_103 & Implementato da \texttt{\seqsplit{test\_returns\_true\_on\_success}}; verifica che l'adapter, quando viene aggiunto correttamente un valore \texttt{User}, restituisca \texttt{True}. & Valore booleano & S \\
	\hline
	TU\_104 & Implementato da \texttt{\seqsplit{test\_commits\_on\_success}}; verifica che l'adapter, quando viene aggiunto correttamente un valore \texttt{User}, chiami il metodo \texttt{commits} del database. & Valore booleano & S \\
	\hline
	TU\_105 & Implementato da \texttt{\seqsplit{test\_returns\_false\_on\_exception}}; verifica che l'adapter, quando non viene aggiunto correttamente un valore \texttt{User}, restituisca \texttt{False}. & Valore booleano & S \\
	\hline
	TU\_106 & Implementato da \texttt{\seqsplit{test\_rollback\_on\_exception}}; verifica che l'adapter, quando non viene aggiunto correttamente un valore \texttt{User}, chiami il metodo \texttt{rollback} del database. & Valore booleano & S \\
	\hline
	TU\_107 & Implementato da \texttt{\seqsplit{test\_password\_is\_hashed}}; verifica che l'adapter, quando viene aggiunto correttamente un valore \texttt{User}, chiami il metodo \texttt{hash}. & Valore booleano & S \\
	\hline
	TU\_108 & Implementato da \texttt{\seqsplit{test\_returns\_true\_on\_success}}; verifica che l'adapter, quando viene eliminato correttamente un valore \texttt{User}, restituisca \texttt{True}. & Valore booleano & S \\
	\hline
	TU\_109 & Implementato da \texttt{\seqsplit{test\_returns\_false\_on\_exception}}; verifica che l'adapter, quando non viene eliminato correttamente un valore \texttt{User}, restituisca \texttt{False}. & Valore booleano & S \\
	\hline
	TU\_110 & Implementato da \texttt{\seqsplit{test\_rollback\_on\_exception}}; verifica che l'adapter, quando non viene eliminato correttamente un valore \texttt{User}, chiami il metodo \texttt{rollback} del database. & Valore booleano & S \\
	\hline
	TU\_111 & Implementato da \texttt{\seqsplit{test\_commits\_on\_success}}; verifica che l'adapter, quando viene eliminato correttamente un valore \texttt{User}, chiami il metodo \texttt{commits} del database. & Valore booleano & S \\
	\hline
	TU\_112 & Implementato da \texttt{\seqsplit{test\_returns\_utente\_when\_found}}; verifica che il metodo \texttt{find_by_username} dell'adapter, quando presente un \texttt{Utente} con il valore \texttt{username} richiesto nel database, restituisca l'oggetto \texttt{User} corrispondente. & Valore di tipo \texttt{User}& S \\
	\hline
	TU\_113 & Implementato da \texttt{\seqsplit{test\_returns\_none\_when\_not\_found}}; verifica che il metodo \texttt{find_by_username} dell'adapter, quando non presente un \texttt{Utente} con il valore \texttt{username} richiesto nel database, restituisca \texttt{None}. & Valore di tipo \texttt{None}& S \\
	\hline
	TU\_114 & Implementato da \texttt{\seqsplit{test\_delegates\_to\_executor}}; verifica che il metodo \texttt{find_by_username} dell'adapter, chiami il metodo \texttt{execute_one_raw}. & Valore booleano & S \\
	\hline
	TU\_115 & Implementato da \texttt{\seqsplit{test\_returns\_utente\_when\_found}}; verifica che il metodo \texttt{find_by_email} dell'adapter, quando presente un \texttt{Utente} con il valore \texttt{email} richiesto nel database, restituisca l'oggetto \texttt{User} corrispondente. & Valore di tipo \texttt{User} & S \\
	\hline
	TU\_116 & Implementato da \texttt{\seqsplit{test\_returns\_none\_when\_not\_found}}; verifica che il metodo \texttt{find_by_email} dell'adapter, quando non presente un \texttt{Utente} con il valore \texttt{email} richiesto nel database, restituisca \texttt{None}. & Valore di tipo \texttt{None} & S \\
	\hline
	TU\_117 & Implementato da \texttt{\seqsplit{test\_delegates\_to\_executor}}; verifica che il metodo \texttt{find_by_email} dell'adapter, chiami il metodo \texttt{execute_one_raw}. & Valore booleano & S \\
	\hline
	TU\_118 & Implementato da \texttt{\seqsplit{test\_returns\_true\_on\_success}}; verifica che l'adapter, quando viene salvato correttamente un valore \texttt{User}, restituisca \texttt{True}. & Valore booleano & S \\
	\hline
	TU\_119 & Implementato da \texttt{\seqsplit{test\_returns\_false\_on\_failure}}; verifica che l'adapter, quando non viene salvato correttamente un valore \texttt{User}, restituisca \texttt{False}. & Valore booleano & S \\
	\hline
	TU\_120 & Implementato da \texttt{\seqsplit{test\_delegates\_to\_executor}}; verifica che il metodo \texttt{save} dell'adapter, chiami il metodo \texttt{mutate_one_raw}. & Valore booleano & S \\
	\hline
	TU\_121 & Implementato da \texttt{\seqsplit{test\_password\_is\_hashed}}; verifica che il metodo \texttt{save} dell'adapter, chiami il metodo \texttt{hash}. & Valore booleano & S \\
	\hline
	TU\_122 & Implementato da \texttt{\seqsplit{test\_returns\_username\_with\_valid\_credentials}}; verifica che il metodo \texttt{check_user} della classe \texttt{UserService} restituisca il valore \texttt{username} dell'oggetto \texttt{User} passato come parametro. & Valore di tipo \texttt{str} & S \\
	\hline
	TU\_123 & Implementato da \texttt{\seqsplit{test\_raises\_when\_user\_not\_found}}; verifica che il metodo \texttt{check_user} della classe \texttt{UserService} sollevi una eccezione di tipo \texttt{InvalidCredentialsError} in caso di utente non trovato nel databse. & Eccezione di tipo \texttt{InvalidCredentialsError} & S \\
	\hline
	TU\_124 & Implementato da \texttt{\seqsplit{test\_raises\_when\_password\_wrong}}; verifica che il metodo \texttt{check_user} della classe \texttt{UserService} sollevi una eccezione di tipo \texttt{InvalidCredentialsError} in caso di password errata. & Eccezione di tipo \texttt{InvalidCredentialsError} & S \\
	\hline
	TU\_125 & Implementato da \texttt{\seqsplit{test\_succeeds\_with\_valid\_data}}; verifica che il metodo \texttt{register_user} della classe \texttt{UserService} non sollevi eccezioni in caso di credenziali valide. & Valore booleano & S \\
	\hline
	TU\_126 & Implementato da \texttt{\seqsplit{test\_raises\_when\_username\_exists}}; verifica che il metodo \texttt{register_user} della classe \texttt{UserService} sollevi una eccezione di tipo \texttt{UsernameAlreadyExistsError} in caso di username già presente nel database. & Eccezione di tipo \texttt{UsernameAlreadyExistsError} & S \\
	\hline
	TU\_127 & Implementato da \texttt{\seqsplit{test\_raises\_when\_email\_domain\_invalid}}; verifica che il metodo \texttt{register_user} della classe \texttt{UserService} sollevi una eccezione di tipo \texttt{InvalidEmailFormatError} in caso di email non valida . & Eccezione di tipo \texttt{InvalidEmailFormatError} & S \\
	\hline
	TU\_128 & Implementato da \texttt{\seqsplit{test\_raises\_when\_email\_exists}}; verifica che il metodo \texttt{register_user} della classe \texttt{UserService} sollevi una eccezione di tipo \texttt{EmailAlreadyExistsError} in caso di email già presente nel database. & Eccezione di tipo \texttt{EmailAlreadyExistsError} & S \\
	\hline
	TU\_129 & Implementato da \texttt{\seqsplit{test\_raises\_when\_add\_user\_fails}}; verifica che il metodo \texttt{register_user} della classe \texttt{UserService} sollevi una eccezione di tipo \texttt{UserCreationError} in caso di errore nella aggiunta dell'utente al database. & Eccezione di tipo \texttt{UserCreationError} & S \\
	\hline
	TU\_130 & Implementato da \texttt{\seqsplit{test\_succeeds\_when\_user\_exists}}; verifica che il metodo \texttt{delete_user} della classe \texttt{UserService} non sollevi eccezioni in caso di eliminazione di un utente andata a buon fine. & Valore booleano & S \\
	\hline
	TU\_131 & Implementato da \texttt{\seqsplit{test\_raises\_when\_user\_not\_found}}; verifica che il metodo \texttt{delete_user} della classe \texttt{UserService} sollevi una eccezione di tipo \texttt{UserNotFoundError} in caso di richiesta di eliminazione di un utente non presente nel database. & Eccezione di tipo \texttt{UserNotFoundError} & S \\
	\hline
	TU\_132 & Implementato da \texttt{\seqsplit{test\_raises\_when\_delete\_fails}}; verifica che il metodo \texttt{delete_user} della classe \texttt{UserService} sollevi una eccezione di tipo \texttt{UserDeletionError} in caso di errore nella eliminazione dell'utente dal database. & Eccezione di tipo \texttt{UserDeletionError} & S \\
	\hline
	\caption{Test di Unità Backend}
\end{longtable}
