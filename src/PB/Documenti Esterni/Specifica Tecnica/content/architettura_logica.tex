\section{Architettura Logica}
L'architettura logica del progetto$^G$ SmartOrder adotta un approccio esagonale.\\
L'obiettivo è quello di mantenere separata la logica di business dalle componenti esterne, come l'interfaccia utente e il sistema di persistenza dei dati.\\
La comunicazione tra la parte di presentazione e la logica applicativa avviene tramite interfacce, che permettono ai controller (ad esempio quelli esposti tramite API$^G$ HTTP) di invocare le funzionalità$^G$ del sistema senza dipendere direttamente dalla loro implementazione.\\
Allo stesso modo, l'accesso al database$^G$ e ad altri servizi esterni è gestito tramite componenti dedicati, che permettono alla logica di business di rimanere indipendente dalle tecnologie utilizzate.

\subsection{Pattern di architettura esagonale}
Il pattern esagonale è stato scelto per organizzare il sistema in modo chiaro e modulare, separando le responsabilità tra le diverse componenti.\\
Questo approccio rende il sistema più facile da mantenere, testare ed estendere nel tempo.\\
I principali elementi dell'architettura esagonale$^G$ sono:\\
\begin{itemize}
	\item \textbf{Core domain}: contiene la logica di business dell'applicazione ed è indipendente dalle tecnologie esterne.
	\item \textbf{Porte}: rappresentano i punti di contatto tra il dominio e l'esterno, e definiscono le operazioni disponibili. Si distinguono in:
	\begin{itemize}
		\item \textbf{Inbound ports}: interfacce che espongono le funzionalità$^G$ del dominio verso l'esterno.
		\item \textbf{Outbound ports}: interfacce che definiscono come il dominio può utilizzare servizi esterni.
	\end{itemize}
	\item \textbf{Adapters}: sono implementazioni concrete delle porte che collegano il dominio alle tecnologie utilizzate.
	\begin{itemize}
		\item \textbf{Inbound adapters}: adattatori che convertono le richieste esterne nel formato atteso del dominio.
		\item \textbf{Outbound adapters}: adattori che implementano le interfacce di uscita collegandole a tecnologie specifiche.
	\end{itemize}
\end{itemize}

\newpage

\section{Servizi principali}
\begin{itemize}
	\item Autenticazione e registrazione
\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{SpecificaTecnica/auth}
	\caption{Autenticazione e Registrazione}
\end{figure}
	\begin{itemize}
		\item \textbf{Core domain}: contiene la logica dedicata alla registrazione di un utente nuovo oppure all'autenticazione di un utente già esistente. Permette di elaborare i dati ricevuti tramite l'inserimento dei dati dell'utente.
        \item \textbf{Inbound ports}: l’applicazione espone un unico punto di ingresso tramite FastAPI Router, che funge da inbound port. Non sono stati definiti ulteriori livelli di astrazione, poiché l’HTTP driver è sufficiente.
        \item \textbf{Outbound ports}: definiscono i contratti verso l'infrastruttura esterna. \textit{UserRepoPort} espone le operazioni di persistenza sugli utenti, mentre \textit{EmailValidationPort} espone la verifica$^G$ del dominio email tramite DNS. Entrambe le interfacce sono definite nel core domain e implementate da adapter dedicati.
        \item \textbf{Adapters}: implementano l’integrazione con servizi esterni: \textit{UserAdapter} (gestione password/token), \textit{UserRepoAdapter} (persistenza su database$^G$), \textit{EmailValidationAdapter} (validazione$^G$ email).
	\end{itemize}
	\item Chatbot e gestione ordine
	\begin{itemize}
		\item \textbf{Core domain}:
		\item \textbf{Inbound ports}:
		\item \textbf{Outbound ports}:
		\item \textbf{Adapters}:
	\end{itemize}
	\item Visualizzazione storico ordini
	\begin{itemize}
		\item \textbf{Core domain}:
		\item \textbf{Inbound ports}:
		\item \textbf{Outbound ports}:
		\item \textbf{Adapters}:
	\end{itemize}
\end{itemize}