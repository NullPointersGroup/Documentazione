\subsubsection{Frontend}

\begin{figure}[H]
	\centering 
	\includegraphics[width=1\textwidth]{SpecificaTecnica/Storico/StoricoFrontend}
	\caption{Diagramma delle classi per Storico Ordini: Frontend}
\end{figure}

Per la definizione di ComponentNavBar, vedere sezione \ref{navbar}

\subsubsubsection{History}
\begin{figure}[H]
	\centering 
	\includegraphics[width=0.4\textwidth]{SpecificaTecnica/Storico/HistoryView}
	\caption{Storico Ordini: History}
\end{figure}

\paragraph{Descrizione}
Componente React che rappresenta la pagina dello storico degli ordini. \\
Recupera i dati tramite \textit{useHistoryViewModel} e gestisce la visualizzazione della lista degli ordini con supporto alla paginazione e al filtraggio per data. \\
Se l'utente è amministratore, vengono mostrati gli ordini di tutti i clienti. \\
Utilizza i componenti \textit{ComponentOrderRow}, \textit{ComponentOrderDetailsModal} e \textit{ComponentPagination}.

\paragraph{Metodi e funzioni}
\begin{itemize}
	\item \textbf{handleLogout()} - esegue il logout dell'utente chiamando l'endpoint \textit{/api/auth/logout} e reindirizza alla pagina principale.
	\item \textbf{handleProfile()} - apre il pannello del profilo utente impostando \textit{profileOpen} a \textit{true}.
	\item \textbf{resetFiltro()} - azzera i campi \textit{dataInizio} e \textit{dataFine} e ricarica la prima pagina senza filtri.
	\item \textbf{ordiniFiltrati: Order} - restituisce la lista degli ordini filtrata in base alle date \textit{dataInizio} e \textit{dataFine} correntemente impostate.
\end{itemize}

\subsubsubsection{ComponentPagination}
\begin{figure}[H]
	\centering 
	\includegraphics[width=0.4\textwidth]{SpecificaTecnica/Storico/ComponentPagination}
	\caption{Storico Ordini: ComponentPagination}
\end{figure}

\paragraph{Descrizione}
Componente React che renderizza i controlli di paginazione per la lista degli ordini.\\
Riceve la pagina corrente, il numero totale di pagine e una callback per il cambio pagina.\\
Non viene renderizzato se il numero totale di pagine è inferiore o uguale a uno.

\paragraph{Metodi e funzioni}
Il componente non espone metodi propri.\\
La navigazione tra le pagine è gestita interamente tramite la callback \textit{onCambia}, invocata con \textit{pagina~-~1} o \textit{pagina~+~1} alla pressione dei pulsanti \textit{Precedente} e \textit{Successivo} rispettivamente.\\
I pulsanti vengono disabilitati automaticamente quando ci si trova alla prima o all'ultima pagina.

\subsubsubsection{ComponentOrderDetailsModal}
\begin{figure}[H]
	\centering 
	\includegraphics[width=0.4\textwidth]{SpecificaTecnica/Storico/ComponentOrderDetailsModal}
	\caption{Storico Ordini: ComponentOrderDetailsModal}
\end{figure}

\paragraph{Descrizione}
Componente React che renderizza una finestra modale con il dettaglio di un ordine selezionato.\\
Mostra il codice ordine, la data, la lista dei prodotti con le rispettive quantità e, se l'utente è amministratore, il nome del cliente.\\
Per gli utenti non amministratori è presente un pulsante per duplicare l'ordine, con relativa richiesta di conferma.\\
La modale si chiude cliccando sul backdrop o sul pulsante di chiusura.

\paragraph{Metodi e funzioni}
\begin{itemize}
	\item \textbf{handleBackdropClick()} - chiude la modale quando l'utente clicca sul backdrop.
	\item \textbf{contenutoDuplica(): ReactNode} - restituisce il contenuto della sezione duplicazione in base allo stato corrente: il pulsante iniziale, la richiesta di conferma oppure il messaggio di successo.
\end{itemize}

\subsubsubsection{ComponentOrderRow}
\begin{figure}[H]
	\centering 
	\includegraphics[width=0.4\textwidth]{SpecificaTecnica/Storico/ComponentOrderRow}
	\caption{Storico Ordini: ComponentOrderRow}
\end{figure}

\paragraph{Descrizione}
Componente React che renderizza una singola riga della tabella degli ordini.\\
Mostra il codice ordine, la data, il numero di prodotti e un pulsante per aprire il dettaglio.\\
Se l'utente è amministratore, viene mostrata anche la colonna con il nome del cliente.

\paragraph{Metodi e funzioni}
Il componente non espone metodi propri.\\
L'unica interazione è il pulsante \textit{Dettaglio}, che invoca la callback \textit{onApriDettaglio} passando l'ordine corrente.

\subsubsubsection{HistoryViewModel}
\begin{figure}[H]
	\centering 
	\includegraphics[width=0.6\textwidth]{SpecificaTecnica/Storico/HistoryViewModel}
	\caption{Storico Ordini: HistoryViewModel}
\end{figure}

\paragraph{Descrizione}
Hook React che funge da ViewModel per la gestione dello storico ordini.\\
Espone stato e azioni per caricare, visualizzare e duplicare ordini, adattando il comportamento al ruolo dell'utente autenticato.\\
Interagisce con \textit{HistoryModel} per il recupero e la duplicazione dei dati tramite le funzioni \textit{getStoricoCliente}, \textit{getStoricoAdmin} e \textit{duplicaOrdine}.

\paragraph{Metodi e funzioni}
\begin{itemize}
	\item \textbf{caricaPagina(n: number, di: string, df: string)} - carica la pagina \textit{n} degli ordini applicando opzionalmente i filtri per data di inizio e di fine.\\
	Invoca \textbf{getStoricoAdmin} o \textit{getStoricoCliente} in base al ruolo dell'utente.
	\item \textbf{apriDettaglio(ordine: Order)} - imposta \textit{ordineScelto} con l'ordine ricevuto, aprendo il dettaglio.
	\item \textbf{chiudiDettaglio()} - reimposta \textit{ordineScelto} a \textit{null}, chiudendo il dettaglio.
	\item \textbf{duplicaOrdine(codice: string)} - richiede la duplicazione dell'ordine identificato da \textit{codice} tramite \textit{apiDuplicaOrdine}.\\
	In caso di errore aggiorna \textit{erroreDuplica} con il relativo messaggio.
\end{itemize}

\subsubsubsection{HistoryModel}
\begin{figure}[H]
	\centering 
	\includegraphics[width=1\textwidth]{SpecificaTecnica/Storico/HistoryModel}
	\caption{Storico Ordini: HistoryModel}
\end{figure}

\paragraph{Descrizione}
File TypeScript che definisce le interfacce dati e le funzioni di accesso alle API per lo storico ordini.\\
Espone i tipi \textit{Order}, \textit{Product} e \textit{StoricoPage} utilizzati internamente dai componenti, e i tipi \textit{OrderSchema}, \textit{ProductSchema} e \textit{HistoryPageSchema} che rispecchiano il formato restituito dal backend.\\
Le funzioni di accesso alle API gestiscono i parametri di paginazione e filtraggio per data.

\paragraph{Metodi e funzioni}
\begin{itemize}
	\item \textbf{getStoricoCliente(pagina: number, perPagina: number, dataInizio?: string, dataFine?: string): HistoryPageSchema} - recupera lo storico ordini dell'utente autenticato tramite \textit{GET /api/storico/miei}, applicando opzionalmente i filtri per data.
	\item \textbf{getStoricoAdmin(pagina: number, perPagina: number, dataInizio?: string, dataFine?: string): HistoryPageSchema} - recupera lo storico ordini di tutti gli utenti tramite \textit{GET /api/storico/tutti}, applicando opzionalmente i filtri per data.
	\item \textbf{duplicaOrdine(codiceOrdine: string)} - duplica un ordine esistente tramite \textit{POST /api/storico/duplica/\{codiceOrdine\}}.\\
	In caso di risposta non positiva lancia un errore con il messaggio restituito dal backend.
\end{itemize}
